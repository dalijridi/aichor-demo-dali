kind: AIchorManifest
spec:
 operator: slurm
 command: export NXF_HOME=/mnt/shared && echo $JAVA_HOME && echo $PATH && nextflow run process_outputs.nf -with-report
 types:
  job:
   ntasks: 1
   nodes: 1 
   cpus_per_task: 2
   memory_per_cpu: 1G
