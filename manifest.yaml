kind: AIchorManifest
spec:
 operator: slurm
 command: chmod -R 777 . && nextflow run process_outputs.nf -with-report
 types:
  job:
   ntasks: 1
   nodes: 1 
   cpus_per_task: 2
   memory_per_cpu: 1G
