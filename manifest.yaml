kind: AIchorManifest
spec:
 operator: slurm 
 #command: python3 ./bnt-demo./cpu_load.py
 #command: module load Java && nextflow run process_outputs.nf -with-report $AICHOR_OUTPUT_BUCKET_NEXTFLOW_PATH
 command: export JAVA_HOME=/home/shared/apps/software/Java/17.0.6 && export NXF_HOME=/mnt/shared/aichor_nextflow && nextflow run process_outputs.nf -with-report $AICHOR_OUTPUT_BUCKET_NEXTFLOW_PATH
 #command: echo $JAVA_HOME && echo $MODULEPATH && nextflow run process_outputs.nf -with-report $AICHOR_OUTPUT_BUCKET_NEXTFLOW_PATH
 types:
  job:
   ntasks: 2
   nodes: 1
   cpus_per_task: 1
   memory_per_cpu: 1G
