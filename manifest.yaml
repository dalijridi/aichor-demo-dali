kind: AIchorManifest
spec:
 operator: slurm
 #command: nextflow run process_outputs.nf -with-report $AICHOR_OUTPUT_BUCKET_NEXTFLOW_PATH
 command: pwd >> /mnt/shared/DEBUG_PWD.txt 
 # && chmod +x gpu_load.sh && ./gpu_load.sh
 types:
  job:
   ntasks: 1   
   #nodes: 1
   cpus_per_task: 1
   memory_per_cpu: 1G
   time: 01:00:00
   #partition: compute
   #constraint: g5xlarge
   #gres: gpu:1
