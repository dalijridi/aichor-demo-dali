kind: AIchorManifest
spec:
 operator: slurm
 command: REPO_DIR=$(pwd) && echo $REPO_DIR && ls -la && nextflow run ${REPO_DIR}/process_outputs.nf -with-report
 types:
  job:
   ntasks: 1
   nodes: 1 
   cpus_per_task: 2
   memory_per_cpu: 1G
