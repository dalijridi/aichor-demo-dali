kind: AIchorManifest
spec:
 operator: slurm
 command: echo $JAVA_HOME && echo $PATH && echo $NXF_HOME && nextflow run process_outputs.nf -work-dir /mnt/shared/work -with-report
 types:
  job:
   ntasks: 1
   nodes: 1 
   cpus_per_task: 2
   memory_per_cpu: 1G
