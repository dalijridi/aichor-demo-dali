kind: AIchorManifest
spec:
 operator: slurm
 command: cd /mnt/shared && echo "Java Home: $JAVA_HOME" && echo "Path: $PATH" && nextflow run process_outputs.nf -with-report
 types:
  job:
   ntasks: 1
   nodes: 1 
   cpus_per_task: 2
   memory_per_cpu: 1G
