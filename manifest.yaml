kind: AIchorManifest
spec:
 operator: slurm
 command: echo $JAVA_HOME && echo $PATH && echo $NXF_HOME && REPO_DIR=$(pwd) && cd /mnt/shared && nextflow run ${REPO_DIR}/process_outputs.nf -with-report
 types:
  job:
   ntasks: 1
   nodes: 1 
   cpus_per_task: 2
   memory_per_cpu: 1G
