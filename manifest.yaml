kind: AIchorManifest
spec:
 operator: slurm
 command: nextflow run process_outputs.nf -with-report $AICHOR_OUTPUT_BUCKET_NEXTFLOW_PATH
 types:
  job:
   ntasks: 2
   nodes: 1
   cpus_per_task: 1
   memory_per_cpu: 1G
