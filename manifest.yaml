kind: AIchorManifest
spec:
 operator: slurm
 command: echo NXF_HOME && echo $NXF_HOME && nextflow run process_outputs.nf
 types:
  job:
   ntasks: 1
   nodes: 1 
   cpus_per_task: 2
   memory_per_cpu: 1G
